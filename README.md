Some containers to do interesting stuff.

# gdb

To avoid having to install gdb on my PC, use a container to debug coredump files.

# firefox-podman

To run a sandboxed firefox (no DRI and no sound/alsa).
